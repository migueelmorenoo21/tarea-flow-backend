spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tareaflow
    username: postgres
    password: Bucanero01
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
